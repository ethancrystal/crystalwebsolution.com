# CRM End-to-End Goal Checkpoint

- **Objective:** Complete and verify the Crystal Web Solution CRM end to end, then add the approved auth switch to login and signup.
- **Status:** blocked
- **Constraints:** Preserve the existing CRM architecture, roles, RLS, storage, lifecycle, single preview/production database, and unrelated user work. Do not send real email, apply production migrations, merge, or deploy without approval.
- **Validation:** `pnpm test:crm`; `pnpm test`; `pnpm build`; `git diff --check`; live Supabase/RLS checks when safe preview credentials are available; browser checks for public and authenticated role flows.
- **Completed:** Audited the repository and live Supabase surface; created draft PR #97 on `feat/crm-end-to-end`; synchronized the branch with the latest `main` baseline; verified the removed `.agents/skills` bundle is absent from the branch and PR comparison; corrected local Supabase URL handling so invalid configuration fails closed; made sign-in return the existing safe configuration error; added admin and staff project company context; corrected the user checkout URL entries in ignored `.env.local` without changing secret values.
- **Evidence:** Fresh CRM suite passes 266/266 including the focused onboarding contracts; fresh full suite passes 400/400; bounded production build passes with `NEXT_TELEMETRY_DISABLED=1 NEXT_PRIVATE_BUILD_WORKER=1 NODE_OPTIONS=--max-old-space-size=1024 pnpm build`; `git diff --check` and `git diff origin/main...HEAD --check` pass; the isolated worktree is clean before commit at `06e124a`; PR #97 is open/draft with GitHub build and Vercel checks successful. Public `/login` renders locally with all three portal links. The approved onboarding slice now adds a dynamic server-gated `/onboarding` route, client-only onboarding action calling the existing `onboard_client_company` RPC, dashboard redirect/no-flicker guard, and focused tests. Sentry preview verification returned the expected generic 500, but no matching event was found in Sentry and the Vercel runtime query found no retained logs.
- **Current blocker:** End-to-end tenant/RLS and authenticated role browser verification remain blocked without disposable preview identities/project/thread IDs or an approved browser takeover. `pnpm test:db` is blocked by `ECONNREFUSED 127.0.0.1:54322` because Docker/local Postgres is not running. `CRM_PREVIEW_ENVIRONMENT=preview pnpm crm:verify:preview` is blocked at the missing `SUPABASE_ANON_KEY` fixture. Sentry event ingestion remains unverified because the fixed preview event is absent from the project search and Hobby runtime-log retention has expired. No production migration was added or applied. The auth-switch phase remains deferred until CRM acceptance is verified.
- **Retry count:** 0/3 for the current credential/database blocker; 0/10 total loop cycles for this checkpoint.
- **Next action:** Obtain one safe verification path: (a) start local Supabase/Docker and run `pnpm test:db`, (b) provide disposable preview fixture values for the harness (never customer credentials), or (c) provide authenticated browser takeover. Then rerun tenant/RLS, role, onboarding completion, and authenticated browser acceptance. Do not merge, deploy production, apply migrations, send real email, or remove the temporary Sentry route without explicit approval.
- **Stop condition:** All acceptance checks pass, the final diff is scoped and clean, and the draft PR contains the verified CRM plus auth-switch changes; or pause if further progress requires credentials, production access, or an irreversible action.
- **Last updated:** 2026-08-24 (approved onboarding implementation, fresh CRM/full tests, bounded build, PR/deployment, RLS, and Sentry audit)
