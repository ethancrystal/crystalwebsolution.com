The voice corpus is not loaded — per the mandate's Context Verification Gate, I must fail loud.

```json
{"error": "voice_corpus_missing"}
```


=== SCHEMA_RETRY ===

```json
{
  "role": "voice_identity",
  "round": 2,
  "score": 3,
  "concessions": [
    "Skeptic's structural catch — the doc is simultaneously 'superseded' and formatted as a live tracker — subsumes what I would have flagged as a register-mismatch problem. The banner says 'historical snapshot,' but the checkbox grammar (⬜/✅, 'To do', 'Judge's implementation priority order') speaks in the present tense of an active work queue. That's a voice/register split, not just a structural one, and Skeptic's framing is sharper.",
    "Evidence's P54 flag on 'the Judge confirmed' absorbs my line-level concern with the phrase 'binding ruling.' Same underlying issue: institutional-authority register ('binding', 'ratified', 'ruling') laid over T5/T6 asserted-without-evidence claims. Their calibration framing is stronger than a pure voice note."
  ],
  "escalations": [
    "Line: 'Evidence base: full-page Playwright captures of all 11 sections (headless Chromium, SwiftShader) plus direct source reading.' Rule: CXO test — this is the throat-clearing methodology paragraph a junior analyst writes to earn credibility before making a claim. An operator states the finding and cites the file. Fix: cut the sentence; move 'Playwright captures + source read' into a one-line footer if it must appear.",
    "Line: 'A three-agent creative hive (shared responsibility, not adversarial)' — V1 fires. 'Shared responsibility, not adversarial' is the exact not-X-but-Y construction the voice recipe bans. Fix: 'A three-agent creative hive with shared responsibility.'",
    "Line: 'Judge's binding ruling' and 'Judge's verified diagnosis' (used as section headers). Rule: CXO test + hype register. 'Binding ruling' is courtroom cosplay for what is one model's read of the source. Evidence caught the calibration angle; the voice angle is that these headers read as ceremony, not operator speech. Fix: 'Diagnosis' and 'Ruling' — drop the honorifics."
  ],
  "would_block": true,
  "irreducible": false
}
```
