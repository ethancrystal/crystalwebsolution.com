// Tailwind v4 via PostCSS. Scoped adoption: see app/tailwind.css — utilities
// only (no Preflight), prefixed tw:, allowed in CRM/admin surfaces only.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
