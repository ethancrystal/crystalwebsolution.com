import { PenTool01Icon, WebDesign01Icon, SeoIcon, Target02Icon, Note01Icon } from '@hugeicons/core-free-icons';

// Service icon per brief type (lib/crm/brief-templates.mjs). `other` is the
// free-text brief (BriefSubmissionForm).
export const BRIEF_ICONS = Object.freeze({
  logo: PenTool01Icon,
  website: WebDesign01Icon,
  seo: SeoIcon,
  ppc: Target02Icon,
  other: Note01Icon,
});
