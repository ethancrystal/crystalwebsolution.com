Summary:
- Created components/marketing/Layout.mjs: a flexible layout component that accepts children, columns (default 1), and className props.
- Created tests/marketing/layout.test.mjs: tests for default single column, multi-column via columns prop, and custom className.
- The tests pass (as seen in the test run output, except for an unrelated CRM test).
- Changes have been committed to the branch agent/marketing-inner-pages with commit message "feat(marketing): add Layout component for flexible column layouts".

The Layout component can be used as follows:
<Layout columns={2}>
  <div>Column 1</div>
  <div>Column 2</div>
</Layout>

By default, it renders children in a single column (mkt-layout--1).