# Handoff — Codex work, July 20 2026

Everything below is pushed to `https://github.com/moizj00/crystalwebsolution.com.git`. To continue on another computer:

```bash
git clone https://github.com/moizj00/crystalwebsolution.com.git
cd crystalwebsolution.com
git checkout codex/contentful   # main work branch for today
```

## Branches from today

### `codex/contentful` (tip `02c782a`) — the primary branch
Content + motion work on top of `main`:

- `9b4f225` feat: implement website content and reviews
- `7eed50b` fix: smooth responsive flying carousel and update branding
- `7e4786c` docs: add Claude branch handoff (see `CLAUDE_HANDOFF.md` — detailed rules/contracts)
- `02c782a` refactor: replace placeholder content with verified CWS records

Highlights vs `main` (~1,434 insertions):
- New `/reviews` page (`app/reviews/page.jsx`) backed by new `lib/reviews.js` (242 lines) and `lib/clients.js`
- Removed placeholder `lib/projects.js` case-study content; content held to "verified records only" client rule
- Flying carousel timing contract in `lib/flyingCarouselLayout.mjs` + `lib/motionStudies.mjs` (see CLAUDE_HANDOFF.md — do not redesign)
- Branding: new `public/crystal-web-solution-logo.svg` / `-icon.svg`, Nav/Menu updates
- Content plan docx: `Crystal-Web-Solution-Complete-Website-Content-Plan.docx`
- Tests: `tests/content.test.mjs`, expanded `tests/latestFeatures.test.mjs`
- SEO: sitemap, layout metadata updates; new embroidery/screen-printing vertical page tweaks

### `codex/plan-website-content-document` (tip `a6cb89e`)
- `a6cb89e` Remove obsolete application files — strips the old app skeleton (~3,418 deletions); planning-oriented branch, not the one to build from.

## Key context for continuing

- **Client rule:** nothing generic/dummy; clients, awards, dates, metrics must be real. Whole app held to this standard.
- **Read `CLAUDE_HANDOFF.md` on `codex/contentful` first** — it documents the Motion/carousel renderer policy (desktop WebGL, <768px SVG/SMIL fallback, reduced-motion static grid, `?motion=full#motion` preview) and exact timing values. Do not add a second ScrollTrigger/RAF loop in `Motion.jsx`.
- Local machine quirk (not relevant on a fresh clone): the working copy lives in nested worktrees under `C:\Users\Sales\Crystal Web Solution\`; a local outer-repo branch also named `codex/contentful` holds unpushed submodule-pointer commits (`fb231f2`, `fe7bc70`, `91f0196`). These were intentionally NOT pushed — the remote branch has the real work.
- Local checkout branch name is `codex/contentinous` (typo) tracking `origin/codex/contentful`.

## Verify build
`npm install && npm run dev` (http://localhost:3000); `npm run build` is the main correctness signal. No lint/test scripts in package.json (content tests run via node directly).
