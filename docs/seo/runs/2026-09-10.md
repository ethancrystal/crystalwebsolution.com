# Daily Run — 2026-09-10

**Mode: A** (unblock-first). `google_analytics_profile: null` on the Ubersuggest
project; the run has no Search Console access on the live domain (no GSC
connector available to it; property status not verified). **Measurement is not
live** — there are no traffic, impression or conversion numbers in this log
because none exist. Everything below labelled *est.* is an Ubersuggest estimate,
not first-party data.

**Eight days elapsed since the last run log (2026-09-02).** In that gap the
production domain changed, the Ubersuggest project was replaced, four blog posts
went live and the issue tracker stopped being reachable. Most of this run went on finding
that out and correcting the memory, because every document in `docs/seo/` was
describing a site that no longer exists.

## The headline: the domain moved again

`cdsportswearusa.com` → **`cdsportswearinc.com`**, via PR #174 "v1.26 — Point
production canonical/contact-form domain at cdsportswearinc.com", merged
**2026-09-03** (GitHub `list_commits`, sha `a861aab4`, read 2026-09-10).

Verified three ways rather than one, per Operations Manual §8:

| Check | Result | Tool, date |
|---|---|---|
| `https://www.cdsportswearusa.com/` | **HTTP 404** | WebFetch, 2026-09-10 |
| `https://cdsportswearusa.com/` (apex) | **HTTP 404** | WebFetch, 2026-09-10 |
| DNS for both | Resolve (216.198.79.x / 64.29.17.x) — same ranges the live host resolves to | `getent hosts`, 2026-09-10 |
| `https://www.cdsportswearinc.com/` | Live. Title "Custom Web Design & AI Automation \| CD Sportswear USA"; canonical `https://www.cdsportswearinc.com` | WebFetch, 2026-09-10 |
| `https://www.cdsportswearinc.com/sitemap.xml` | 25 URLs, all on `www.cdsportswearinc.com`, including 4 `/blog/` URLs | WebFetch, 2026-09-10 |
| `crystalwebsolution.com` | No DNS resolution — still dark, as decided | `getent hosts`, 2026-09-10 |

So the previous production host is a **dead end, not a redirect**. It was live
from 2026-08-27 to 2026-09-03; anything indexed or linked in that week now hits
a 404. That is the first item on the blocked list.

## Metric table

Deltas are against the 2026-09-02 run log. Blank = not measured.

| Metric | Source | 2026-09-02 | 2026-09-10 | Delta / note |
|---|---|---|---|---|
| Live host | WebFetch | `www.cdsportswearusa.com` | `www.cdsportswearinc.com` | **Changed 2026-09-03** (PR #174) |
| cdsportswearinc.com — DA | Ubersuggest `backlinks_overview` (est.) | — | 1 | New host, no history |
| cdsportswearinc.com — backlinks | same (est.) | — | **0** | |
| cdsportswearinc.com — referring domains | same (est.) | — | **0** | |
| cdsportswearinc.com — organic keywords | — | — | | `domain_overview` returned **HTTP 403** this run; not measured |
| cdsportswearusa.com — DA | Ubersuggest `domain_overview`, locId 2840 (est.) | `noData` | 1 | Domain now has data, and 404s |
| cdsportswearusa.com — backlinks | same (est.) | `noData` | 6 | All spam, all nofollow |
| cdsportswearusa.com — referring domains | same (est.) | `noData` | 3 | All spam — see below |
| cdsportswearusa.com — organic keywords | same (est.) | `noData` | 0 | |
| crystalwebsolution.com | — | DA 9, 212 backlinks, 45 ref domains | | Not re-measured. Domain is dark; not this operation's concern |
| Tracked keywords | Ubersuggest `get_project` | 6 / 125 (project `5dfd943c…`) | **54 / 125** (project `109eb168…`) | **Project replaced 2026-09-09** |
| Tracked competitors | same | 0 / 5 | **5 / 5** | huemor.rocks, barrelny.com, ironpaper.com, loungelizard.com, bopdesign.com |
| GA connected | same | no | no | `google_analytics_profile: null` |
| Blog posts live | Supabase SQL, `blog_posts` | 0 | **4** | All `status: published` |

### Keyword drift (Ubersuggest `keyword_overview`, US locId 2840, all est.)

| Keyword | 2026-09-02 | 2026-09-10 | |
|---|---|---|---|
| hire shopify developer | 880 / diff 26 | 880 / **diff 37** | Volume and CPC unchanged |
| ai automation agency | 4,400 / diff 35 | 4,400 / **diff 40** | Was 38 on 2026-08-21. Oscillating up |
| hire a shopify developer | 880 / diff 8 | 880 / diff 8 | |
| rfp web development | 260 / diff 13 | 260 / diff 13 | |
| website development rfp | 260 / diff 13 | 260 / diff 13 | |
| web development northern virginia | 260 / diff 9 | 260 / diff 9 | |

Neither drift changes a build decision: `hire a shopify developer` (diff 8) is
still the anchor for `/hire/shopify-developer`, and `ai automation agency` was
already Monitor-only.

## Publication pipeline (ran first, as required)

**Four posts are live** — none of them through this repository's pipeline.
`.github/workflows/` still contains only `docker-ci.yml` (2026-09-10); the
publish workflow built on 2026-09-02 is still parked at
`docs/seo/seo-publish-blog.yml.pending` and **has never run**. The rows reached
`blog_posts` by some other route.

| Slug | `published_at` | Status | Verified live |
|---|---|---|---|
| web-development-rfp-guide | 2026-09-03 01:49 UTC | published | **Yes** — renders, H1 correct, self-canonical to `https://www.cdsportswearinc.com/blog/web-development-rfp-guide`; its three closing internal links point at `www.cdsportswearinc.com` (`/contact`, `/services/web-design`, `/process`) though the targets themselves were not fetched (WebFetch, 2026-09-10) |
| how-much-does-ai-automation-cost | 2026-09-06 09:10 UTC | published | In `/sitemap.xml`; page not individually fetched this run |
| branding-and-web-design-studio | 2026-09-06 09:26 UTC | published | In `/sitemap.xml`; page not individually fetched this run |
| web-design-manassas-va | 2026-09-06 09:32 UTC | published | In `/sitemap.xml`; page not individually fetched this run |

Source: `blog_posts` via Supabase SQL on project `wmnjosiikehsuaqucvja`,
2026-09-10; sitemap via WebFetch, 2026-09-10.

**Consequence for the drafts directory:** it is no longer a reliable index of
what is live. `docs/seo/drafts/blog/` contains one draft
(`web-development-rfp-guide.md`) plus `README.md` and `assets/`, and holds no
draft for the other three live posts as of 2026-09-10. Operations Manual §5 now says to read `blog_posts`
directly on every run.

**T+7d checkpoint for the RFP guide falls today and cannot be run.** The run has
no Search Console access on `cdsportswearinc.com`, and the one Ubersuggest call
that would have shown organic keywords for the host (`domain_overview`) returned
HTTP 403 (2026-09-10). T+7d cannot be evidenced either way. The §10 framework is
blocked at T+0 for every page until MJ connects GSC.

**MJ: submit all four blog URLs plus the homepage in Search Console** once a
property exists on `https://www.cdsportswearinc.com`.

## Lane worked: Backlinks

Chosen over Content because the PBN rule requires every new referring domain to
be checked and flagged the same day, and this was the first run since three
appeared.

### New referring domains — all three are spam

Ubersuggest `linking_domains` / `backlinks` for `cdsportswearusa.com`,
2026-09-10. Checked against `backlinks/pbn-watch.md`: **no match on signature A**
(the 2026-08 blast), but they are a second network of the same class, now
recorded as **signature B**:

| Domain | DA (est.) | Gained | nofollow |
|---|---|---|---|
| white-hat-high-authority-seo.shop | 2 | 2026-09-01 | true |
| seo-backlinks-contextual-dofollow.site | 2 | 2026-09-01 | true |
| quality-manual-link-building.shop | 2 | 2026-08-31 | true |

Shared fingerprint: path `/<hyphenated-slug>-<8 chars>/page-75`; title "Buy
High-Quality Contextual Backlinks for Higher Google & AI Rankings <date> —
<host>"; anchor naming the vendor **`easyrank.link`**; all pointing at the bare
root `https://cdsportswearusa.com/`.

**Assessment: watch, do not disavow.** The links are nofollow, the sources have
DA 2, and their target has served 404 since 2026-09-03 — they land nowhere. The
live host has 0 backlinks and 0 referring domains, so the network has not
followed the move. Nothing to file, and no GSC property to file it from.
Recheck every Backlinks lane; escalate if the `easyrank.link` anchor appears on
`cdsportswearinc.com` or the links turn dofollow.

Normally this would be flagged on CRY-29 the same run. **The Linear team is not
reachable** (see below), so it is recorded here and in `pbn-watch.md` instead.

### Prospects researched: 10, plus 12 checked and rejected

Written to `backlinks/prospects.md` with the specific page, our target URL, the
reason their reader benefits, the route a link is actually obtained by, and the
evidence checked on 2026-09-10. Research only — nothing sent.

Rejections cover 17 domains across 12 entries. Strongest three: **nonprofitwp.org** (its RFP page already links out to a
competitor's RFP guide as the "if you must" alternative — a visible, self-
declared slot for ours), **choosemanassas.org** (the city EDA's start-a-business
page already links to private providers), and **princewilliamliving.com** (DA 43
est., highest-authority local prospect that costs nothing, with a stale resource
roundup containing a dead link — a real reason to make contact).

**Two prospects cost money and need MJ's approval before anyone acts:** Prince
William Chamber membership (dues not published) and NTEN membership. Both are
flagged in the file. Six of the rejected domains were guest-post farms that sell
placements; one, `pwcgmcc.org`, is a compromised site carrying injected gambling
spam and should not be approached at all.

## Artifacts

GitHub write is still refused (`403 Resource not accessible by integration` on
`POST /git/refs`, 2026-09-10), so **no PR could be opened.** The five files below
were produced in full and handed to MJ outside the repository — as a
`git format-patch` file whose base blobs were checked byte-for-byte against
`main` at `093c55b7`, plus a zip of the finished files:

| File | Change |
|---|---|
| `docs/seo/OPERATIONS-MANUAL.md` | **v3.** §1 systems table re-verified live; §2 mode logic points at the new project and says what to do when a project 404s; §3 records the strategy split; §4 adds paid memberships and domain changes as gates; §5 says to read `blog_posts` directly; §7 records signature B; §10 notes the blocked checkpoints; §11 blocked list rewritten; **new §12** documents the domain history and what the move cost |
| `docs/seo/KEYWORD-REGISTRY.md` | New project ID; all target URLs on the new host; eight mapped keywords with page states; drift table; **the 47 tracked-but-unmapped terms clustered**; `/blog/how-much-does-ai-automation-cost` recorded as having no viable term |
| `docs/seo/backlinks/pbn-watch.md` | Signature A retitled; **signature B added** with the three domains, the fingerprint, and the watch-don't-disavow assessment |
| `docs/seo/backlinks/prospects.md` | Replaced the placeholder with 10 researched prospects, 12 rejections, and the paid-placement flags |
| `docs/seo/runs/2026-09-10.md` | This log |

## Strategy findings that need MJ, not more research

1. **54 keywords are tracked; 7 are mapped to a URL.** The project created on
   2026-09-09 tracks mostly broad head terms — 17 variants of "digital marketing
   agency", 14 of "logo/brand identity", 6 SEO terms with no SEO page to send
   them to. That is the opposite of the "specific, low-difficulty, national"
   strategy in §3. The registry clusters all 47 but maps none of them, because
   mapping them would create the many-keywords-one-page collision the registry
   exists to prevent.
2. **`/blog/web-design-manassas-va` shipped against a parked term.** The
   Manassas-first ladder was declared superseded on 2026-09-02; a post went live
   against `web design manassas va` (20/mo, diff 5, est.) on 2026-09-06. Either
   the local ladder is back or that post is an exception. The manual cannot tell.
3. **The brand name is unsettled.** The domain and the artwork say INC (PR #189,
   2026-09-08); `SITE.name` (per that PR's body) and the live homepage title
   still say USA. Other metadata was not inspected. PR #189 left it deliberately,
   calling it an owner decision. No page whose copy names the company should be
   built until it is settled.

## Drafts awaiting MJ approval

None. `docs/seo/drafts/blog/web-development-rfp-guide.md` carries
`approved: true` and its post is live.

## Open PRs awaiting merge

Ten open: #186, #184, #182, #181, #158, #156, #155, #154, #153, #152
(`list_pull_requests`, 2026-09-10 — numbers only; titles and authors were not
captured this run, so none has been classified as SEO or not). None was opened
by this run — GitHub write is refused. Merging is MJ's, always.

## Verification

An independent agent checked all five artifacts against the raw tool output
with no access to this run's reasoning. Verdict: **SHIP WITH REQUIRED CHANGES**
— 16 required, all applied. What it caught:

1. **A number that was never measured.** The manual and this log both said
   "Ubersuggest reports 0 organic keywords for `cdsportswearinc.com`". That
   reading is for `cdsportswearusa.com`; the call for the live host returned
   HTTP 403. Both now say the figure could not be read.
2. **A confirmation put in MJ's mouth.** `pbn-watch.md` said neither spam
   network was bought, "MJ confirmed that on 2026-09-02" — but signature B was
   found on 2026-09-10 and has never been put to MJ. The confirmation is now
   scoped to signature A only.
3. **A smoothed-over gap.** "Six backlinks, all one template": `domain_overview`
   reports 6, the `backlinks` call returned 5. Five were inspected and match;
   the sixth was never returned. Both files now say so.
4. **An invented reason.** The manual explained *why* PR #189 left `SITE.name`
   alone ("renaming touches metadata, JSON-LD and tests"). The PR body says only
   that it is an owner decision. Also struck: claims about the live JSON-LD,
   which was never read.
5. **Deletion asserted from absence.** "The Linear team no longer exists" became
   "is not present in the workspace this run can reach" in all four places —
   `list_teams` cannot tell deletion from a re-pointed connector.
6. **Fetches that did not happen.** `prospects.md` said every asset was
   "verified live"; the RFP guide was fetched, the rest are attested by
   `blog_posts` and the sitemap. The RFP guide's internal links *point at* the
   new host — the targets themselves were not fetched.
7. **Unsourced provenance.** The prospect evidence came from a research agent,
   not from this run's own tool calls. The file now says so in its first line.
8. **PRs characterised without evidence.** "none are SEO … (dependabot)" — only
   the ten numbers were captured. The claim is withdrawn.
9. **The DA rollup trap, twice.** The file catches it for `lodestar.asu.edu`
   then repeats it for `experts.n8n.io`, where DA 54 is the `n8n.io` root. Now
   flagged.
10. **A guess written into standing policy.** §2 told future runs a lone 403 "is
    a rate limit, not an auth failure". Nothing observed distinguishes a rate
    limit from a plan restriction; §2 now says so.
11. **Two lists that had drifted apart** — the blocked list here and in §11.
    Reconciled, with the paid-prospect approval and the carried-forward Supabase
    items added to both. Also fixed: one CPC printed as $17.99 in one file and
    $18.00 in the other (`cpc 17.995`; both now $18.00), and a claim that the
    drafts directory holds "one file" when it holds three entries.

It verified as correct: the 54 → 7 mapped + 47 unmapped keyword arithmetic, with
all six clusters disjoint and every term appearing exactly once; the drift table
against the 2026-09-02 figures; every volume, difficulty and CPC against the raw
`keyword_overview` output; the project `created_at` conversion; that the three
unfetched blog posts are labelled as unfetched; that the 403 and the
publish-pipeline gap are disclosed rather than smoothed; that the two strategy
conflicts are put to MJ rather than resolved by the run; and that **no approval
gate was crossed** — nothing sent, bought, merged, published or disavowed, and
no Ubersuggest project created or deleted.

## Blocked on MJ

Full table in Operations Manual §11. In priority order:

1. **Decide what `cdsportswearusa.com` does** — 301 to `https://www.cdsportswearinc.com`, or detach it. It currently resolves and serves 404.
2. **Grant the GitHub connector Contents + Pull requests write.** Still 403 on 2026-09-10; every run hands over patch files instead of PRs. Biggest single tax on this operation.
3. **Restore an issue queue.** The Linear team *Crystal Web Solution* is not present in the workspace this run can reach — `list_teams` returns only `TMS` (2026-09-10); whether it was deleted or the connector now points elsewhere is unknown. CRY-17…30 are unreachable, and findings have nowhere to go but these files.
4. **Connect GA4 and Search Console** to Ubersuggest project `109eb168…` on the `cdsportswearinc.com` property. Until then every run is Mode A and nothing can be verified past T+0.
5. **Install the blog publish workflow** and its secrets, or tell the run that publishing happens elsewhere and the pipeline should be retired.
6. **Rule on the strategy split** (finding 1 and 2 above).
7. **Settle the brand name** (finding 3).
8. Submit the four blog URLs in Search Console once #4 exists.
9. Two paid prospects need a yes/no: Prince William Chamber, NTEN.
10. Name the Supabase Storage bucket for cover images (`SEO_BLOG_COVERS_BUCKET`) — blocks cover images on drafts.
11. Carried forward, unverified since 2026-09-02: the `drain-crm-outbox` cron row and the Supabase auth redirect allow-list, both of which named the old domain.

## Next-run lane

**Content — `/hire/shopify-developer` (the old CRY-24).** Its anchor term
`hire a shopify developer` is still 880/mo at difficulty 8 (est., 2026-09-10),
it is the only unbuilt Now-tier target that does not depend on the unsettled
strategy questions, and the four live blog posts give it internal links to sit
among. Build it as a code page on branch `seo/hire-shopify-developer` following
`CLAUDE.md`, using `app/embroidery-screen-printing-web-design/page.jsx` as the
structural model. **Write the copy so it does not depend on whether the company
is called USA or INC** until finding 3 is settled.

Then Keywords, to recheck the two drifting difficulties.

## Addendum — 2026-09-11

Two things below are now stale, corrected here rather than rewritten above:

- **"GitHub write is still refused... no PR could be opened."** The MCP
  connector is still 403, but this account also has GitHub connected through
  Zapier (`ethancrystal`, full write access). All five files in this log's
  Artifacts table were pushed and opened as a PR via that route instead of
  delivered as a patch (see the repo's PR list for the number).
  Every blob SHA was verified byte-for-byte against the source before opening it.
- **Strategy finding 3, "the brand name is unsettled."** MJ settled it in
  conversation on 2026-09-11: **CD Sportswear INC**. The codebase rename
  shipped separately as PR #190. See Operations Manual v3.1 §0/§3.

Neither the next-run lane recommendation nor anything else in this log
changes as a result — both are noted here for whoever reads this log next.
