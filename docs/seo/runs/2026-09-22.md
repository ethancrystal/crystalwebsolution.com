# SEO run — 2026-09-22 (daily) — lane: content / brand-drift

Mode: A (measurement dark) — evidence: GSC UNAVAILABLE; Bing UNAVAILABLE;
Firecrawl returned 403 on the sitemap fetch this run. All numbers are
unavailable — not estimated.

Operator: Hermes · model: upstage/solar-pro4:free (provider nous) · workdir:
C:\Users\moizjmj\Crystal Web Solution\.worktrees\update-content

## Publication check
- Live site re-fetched this run: homepage, 8 service pages, /work, /contact,
  /blog, /about, /process, /reviews all HTTP 200, canonical on
  www.cdsportswearinc.com, robots-meta `index, follow`.
- No live changes this run — the edits below are on branch
  `seo/2026-09-22-brand-drift-title-fix`, not merged, not deployed.

## Work shipped
- **Brand-drift fix in 4 marketing-page SEO_TITLE strings.**
  - `app/about/page.jsx` — `SEO_TITLE`
    - Before: `'CD Sportswear Inc— Web Design & Branding Studio'`
    - After:  `'Web Design & Branding Studio — CD Sportswear INC'`
    - Why: "Inc" → "INC"; em-dash had no space before it; reordered topic-first
      so the brand sits last (matching the other absolute-title conventions).
  - `app/contact/page.jsx` — `SEO_TITLE`
    - Before: `'Contact CD Sportswear | Send Us Your Project Brief'`
    - After:  `'Send Your Project Brief — CD Sportswear INC'`
    - Why: missing "INC"; `|` separator → spaced em dash; topic-first.
  - `app/blog/page.jsx` — `SEO_TITLE`
    - Before: `'Web Design & Branding Blog | CD Sportswear Inc'`
    - After:  `'Studio Notes on Web Design & Branding — CD Sportswear INC'`
    - Why: "Inc" → "INC"; `|` → spaced em dash; title now describes the page's
      actual content (the "Studio notes" eyebrow) instead of a generic "Blog"
      label.
  - `app/services/page.jsx` — `SEO_TITLE`
    - Before: `'Web Design, Branding & AI Automation | CD Sportswear'`
    - After:  `'Connected Services — CD Sportswear INC'`
    - Why: missing "INC"; `|` → spaced em dash. Also de-cannibalizes: the old
      title carried three pillar head terms (Web Design, Branding, AI
      Automation) into the /services hub — a non-pillar page. The hub's job is
      to be a hub, not to compete with the nine pillar pages (STRATEGY.md §1).

## Findings (evidence-backed)
- **Brand drift across 4 absolute titles.** Three pages spell the brand
  "CD Sportswear Inc" or "CD Sportswear" in their `<title>` against the
  required exact brand `CD Sportswear INC` (SITE.name in lib/site.js; test
  `the canonical site brand is CD Sportswear INC` in tests/marketing.test.mjs).
  Confirmed by reading the four source files directly (app/about/page.jsx L16,
  app/contact/page.jsx L39, app/blog/page.jsx L15, app/services/page.jsx L16).
- **/services hub title is a cannibalization risk.** The old title
  `"Web Design, Branding & AI Automation | CD Sportswear"` uses the same three
  head terms that the pillar pages own, on a hub page whose job is to link *down*
  to those pillars, not to carry them (STRATEGY.md §1: "/services — No head term
  of its own. Descriptive links to all nine."). Replacing it with
  `"Connected Services — CD Sportswear INC"` removes the overlap.
- **Pages already correct (not touched).** /process, /reviews, /work,
  /embroidery-screen-printing-web-design already use "CD Sportswear INC" in
  their SEO_TITLE strings. All four DESCRIPTION strings already use the correct
  brand. No homepage section copy was touched (journey safety — would desync the
  WebGL camera).

## Contract checks
- Banned-word list (lib/services.mjs + lib/servicePages.mjs + manual scan of
  page files): none of the four new strings contains a banned word.
- Brand spelling after edit: exactly `CD Sportswear INC` in all four titles.
- SEO_SHIP_TABLE in tests/marketing.test.mjs: untouched — none of the 7 pinned
  slugs changed seoTitle or h1.
- SERVICES[].desc / hero mirror rule: not touched (no change to lib/services.mjs
  or lib/servicePages.mjs).
- SITE fields in lib/site.js: not touched.
- Homepage journey: no section added/removed/reordered, no section id changed,
  SERVICES count still 8, IN_MOTION_CARDS still 8, About.jsx WORDS untouched.

## Tests
- `pnpm test:marketing`: 10 test files, 36 tests — all pass.
- `pnpm test`: 516 tests, 0 failures.
- `pnpm build`: production build succeeds; all public marketing routes render
  (static/SSG) with no errors.

## Blocked on MJ
- Merge this PR (#218) into main. The agent does not merge.

## Next lane
Content — homepage cannibalization. The homepage `<title>` in app/layout.jsx is
`"Custom Web Design & AI Automation | CD Sportswear INC"` and the OG/title
default is the same, which overlaps the /services/web-design pillar's
`"Custom Web Design Studio"` and the AI-automation pillar. The homepage's job is
brand + positioning + passing authority down to every pillar, and it must not
carry a pillar head term as its primary. Confirm the rendered `<title>` from
live HTML before acting, then decide whether to switch the homepage default title
to a brand/positioning stem (e.g. "Digital Studio — Websites, Brand, Motion & AI
Automation") without touching any section copy.
