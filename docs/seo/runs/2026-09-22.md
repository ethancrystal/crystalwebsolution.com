# SEO run — 2026-09-22 (daily) — lane: technical

Mode: A (measurement dark) — evidence: no GSC section populated in pre-run data; GA4 not connected to Ubersuggest project (manual §2, §11 #4)

Operator: Hermes cron `CDS-SEO daily` · model: Solar Pro4 (Upstage) · workdir: C:\Users\moizjmj\CD Sportswear USA\site

## Publication check

- Nothing newly live since last run — no drafts flipped, no open PRs deployed. Blog table still holds 4 published posts (manual §1, `blog_posts` 4 rows).
- Pre-run data says mode is A (measurement dark): GA4 not connected, GSC not verified live. No traffic/position numbers to report this run.

## Measurement

Measurement: UNAVAILABLE — Mode A, no GSC or GA4 first-party data reachable this run. No numbers to report; no registry keyword movement to evidence.

Bing (week of 2026-09-15): UNAVAILABLE — pre-run Bing section not populated.

Movements worth noting: none evidenced. Mode A means the run cannot say whether anything moved.

## Work shipped

- **`public/llms.txt`** — machine-readable site summary per llmstxt.org. H1 "CD Sportswear INC", one-paragraph blockquote, then `## Services`, `## Work`, `## Guides`, `## Contact` — each a list of `[Title](absolute URL): one-line summary`. Covers all 29 URLs from the live `https://www.cdsportswearinc.com/sitemap.xml` (fetched 2026-09-22): 10 services, 7 work, 10 guides, 2 contact. No URLs invented; no blog posts omitted; no sameAs/social URLs added to `lib/site.js` (left empty per `lib/site.js` §"LEAVE EMPTY until each profile is real"). File: `public/llms.txt`. Verified by: read of live sitemap (WebFetch, 2026-09-22) cross-checked against llms.txt entry list.
- **IndexNow key** — one 32-character lowercase hex key generated and stored as `public/96c7249bcd1f18d19dabd1f048c79e8b.txt` (single line, no trailing newline). Verified by: `cat public/96c7249bcd1f18d19dabd1f048c79e8b.txt` showing exactly the key.
- **`scripts/seo/indexnow-ping.mjs`** — reads the key from `public/<key>.txt`, builds `{ host, key, keyLocation, urlList }`, POSTs to `https://api.indexnow.org/indexnow` for the URLs passed on the command line. `--dry-run` prints the payload without sending. Usage documented in `docs/seo/OPERATIONS-MANUAL.md` §IndexNow. Verified by: `--dry-run` dry run below — payload printed, no network call made.
- **`app/robots.js`** — existing `User-Agent: *` rule untouched (all 11 Disallow paths preserved exactly: `/login/admin`, `/login/client`, `/login/employee`, `/forgot-password`, `/auth/`, `/dashboard`, `/dashboard/`, `/admin`, `/admin/`, `/team`, `/team/`, `/api/`); sitemap and host lines untouched. Added explicit `allow: "/"` blocks for `GPTBot`, `OAI-SearchBot`, `ChatGPT-User`, `ClaudeBot`, `PerplexityBot`, `Bingbot`, `Applebot`, `Google-Extended`. Verified by: reading `app/robots.js` and confirming the 8 new rules plus the unchanged `*` rule.
- **`docs/seo/OPERATIONS-MANUAL.md`** — two additions: (a) `llms.txt` maintenance rule — update `public/llms.txt` whenever a URL is published, renamed, or removed, and it must always cover every sitemap URL; (b) IndexNow subsection — run `scripts/seo/indexnow-ping.mjs` after every merge that adds or changes a URL, with key storage, `--dry-run`, keyLocation, and scope rules. Verified by: read of the edited section.
- No `sameAs` or social URLs added to `lib/site.js` — `socials: []` left deliberately empty per the existing comment and the task constraint.

PR: https://github.com/ethancrystal/crystalwebsolution.com/pull/201

## Findings (evidence-backed)

- The live sitemap at `https://www.cdsportswearinc.com/sitemap.xml` (WebFetch, 2026-09-22) lists 29 URLs — 6 more than the 23 URLs recorded in the manual §1 table (which said "25 URLs" on 2026-09-10 and the pre-run context cited "29 URLs on 2026-09-19"; the 2026-09-19 manual record and the live fetch agree on 29). The site section counts are: 10 services, 7 work, 10 guides (5 blog + about/process/privacy/terms/embroidery-screen-printing-web-design), 2 contact (contact + reviews) — 29 total. No blog posts from the DB were omitted; the 4 published blog posts are all present.
- AI-crawler robots rules were absent before this run: `app/robots.js` before the patch had only the `*` rule. The 8 AI crawlers now have explicit `allow: "/"` so the policy is deliberate and readable (AI-visibility checklist §A, item 1).
- No IndexNow key existed before this run — `public/` had no `<key>.txt` file and no `scripts/seo/indexnow-ping.mjs`. Both are new.
- `lib/site.js` still has `socials: []` — correct; no off-site profiles are claimed/live yet, so emitting `sameAs` would be an active negative signal (manual §3 positioning caveat; `lib/site.js` comment). Left untouched per task constraint #4.
- Mode A persists: GA4 and GSC still not connected to Ubersuggest project `109eb168…` on the `cdsportswearinc.com` property (manual §11 #4). No page can be verified past T+0 until that is resolved.

## Blocked on MJ

- **Connect GA4 + Search Console to Ubersuggest project `109eb168…` on `cdsportswearinc.com`** — keeps every run in Mode A; no T+7/T+30 verification possible (manual §11 #4).
- **Grant the GitHub MCP connector Contents + Pull requests write on `ethancrystal/crystalwebsolution.com`** — the run uses the terminal `git`/`gh` as the user's authenticated account; the MCP write 403 (manual §11 #2) is a separate concern. Not blocking this run, but worth resolving for the automated job.
- **Decide what `cdsportswearusa.com` should do** — still resolves to Vercel and serves 404; anything indexed in the 2026-08-27 → 2026-09-03 window is a dead end (manual §11 #1, §7). Not touched this run.

## Next lane

Content → `/hire/shopify-developer` — the registry's highest-volume unbuilt target (`hire a shopify developer`, 880/mo, diff 37; `hire a shopify developer`, 880/mo, diff 8 — Ubersuggest, 2026-09-10, §3). A landing page on a `seo/hire-shopify-developer` branch following `CLAUDE.md`, `approved: false` equivalent in the PR, with one target keyword, real answers, and internal links to `/services/web-development` and the RFP guide. Verify with `pnpm test:marketing` + a read of the route. Next run log should record the draft URL and its target keyword.
