# SEO Operations Run Log — 2026-09-22

> Configures this run as an auditable, reproducible unit the cds-seo-operator skill can replay, hand off, and audit later.
> This file and the rest of `docs/seo/` are internal — not a customer deliverable.

## Snapshot

- **When (UTC):** 2026-09-22
- **Goal:** Ship the AI-visibility technical lane for https://www.cdsportswearinc.com as ONE open PR on ethancrystal/crystalwebsolution.com. Keep it OPEN, never merge.
- **Lane:** technical
- **Files in scope:** `public/`, `app/robots.js`, `scripts/seo/`, `docs/seo/`
- **Out of scope:** app/layout.jsx, lib/site.js, components/marketing/*, app/sitemap.js, content rewriting, DNS, domain changes, merge.
- **Environment:** local SE if the skill needs it; otherwise no credentials required — this run touches only files + git + gh + pnpm test:marketing.

## Pre-run data

- Site URL: https://www.cdsportswearinc.com
- Live sitemap URL count: 29 (fetched from https://www.cdsportswearinc.com/sitemap.xml)
- GSC data snapshot: empty — no populated GSC section in the pre-run data.
- Ubersuggest project: connected to Google Analytics but not Google Search Console.
- **Mode:** A (measurement dark) — pre-run data has no GSC section and GA4 is not connected to the Ubersuggest project, so the run proceeds on file/git/CI evidence only.

## What shipped

1. `public/llms.txt` — llmstxt.org format. H1 "CD Sportswear INC", a one-paragraph blockquote of what the studio does and for whom, then sections `## Services`, `## Work`, `## Guides`, `## Contact`, each a list of `[Title](absolute https://www.cdsportswearinc.com URL): one-line summary`. Covers all 29 URLs from the live sitemap, nothing invented.
2. `public/96c7249bcd1f18d19dabd1f048c79e8b.txt` — the IndexNow key, 32-character lowercase hex, single line.
3. `scripts/seo/indexnow-ping.mjs` — reads the key from that key file, POSTs `{ host, key, keyLocation, urlList }` to https://api.indexnow.org/indexnow for the URLs passed on the command line, with a `--dry-run` flag that prints the payload without sending.
4. `app/robots.js` — every existing Disallow path kept exactly as-is, plus explicit `allow: "/"` rules for GPTBot, OAI-SearchBot, ChatGPT-User, ClaudeBot, PerplexityBot, Bingbot, Applebot, Google-Extended. Existing sitemap + host lines preserved.
5. `docs/seo/OPERATIONS-MANUAL.md` — updated with one line that llms.txt must be updated whenever a URL is published, and a short "IndexNow" subsection saying to run the ping script after every merge that adds or changes a URL.
6. `docs/seo/runs/2026-09-22.md` — this file, written from the run-log template.
7. No `sameAs` or social URLs were added to the Organization schema in `lib/site.js`.

Commit: `seo: llms.txt, IndexNow, AI-crawler robots rules (technical lane, 2026-09-22 run)`  
Branch: `seo/2026-09-22-ai-visibility`  
PR: #201 on https://github.com/ethancrystal/crystalwebsolution.com — kept OPEN, never merged.

## How verified

- `git status --short` after the push: clean working tree on `seo/2026-09-22-ai-visibility`.
- `git show --stat HEAD`: lists `public/llms.txt`, `public/96c7249bcd1f18d19dabd1f048c79e8b.txt`, `scripts/seo/indexnow-ping.mjs`, `app/robots.js`, `docs/seo/OPERATIONS-MANUAL.md`, `docs/seo/runs/2026-09-22.md`.
- `git diff origin/main --stat`: only the expected files changed vs `origin/main`.
- `node scripts/seo/indexnow-ping.mjs --dry-run https://www.cdsportswearinc.com/` printed the JSON payload with key `96c7249bcd1f18d19dabd1f048c79e8b` and did not send anything.
- `gh pr view 201 --repo ethancrystal/crystalwebsolution.com --json url,state,headRefName` returned state OPEN and headRefName `seo/2026-09-22-ai-visibility`.
- `pnpm test:marketing` exited 0 and ran 10 test files / 36 tests.
- Manual confirmation that `app/robots.js` still contains all twelve original Disallow paths.

## Errors encountered

- `indexnow-ping.mjs` was initially written with `const fs = require('fs')` / `const path = require('path')`, which is invalid in a `.mjs` file (ESM scope has no `require`). Rewrote it to use `import { readdirSync, readFileSync } from 'node:fs'`, `import { dirname, join, resolve } from 'node:path'`, and `import { fileURLToPath } from 'node:url'` so both `node --dry-run` and any Next.js webpack pass treat it as a normal ESM script.
- A first `gh pr create` body used a `<PR_NUMBER>` placeholder; replaced with the real PR number in a follow-up commit and refreshed the PR body with `gh pr edit --body-file`.

## What was NOT done (and why)

- Did not merge — the goal and constraints both say never merge.
- Did not add sameAs/social URLs to `lib/site.js` — the file deliberately keeps socials empty until each profile is real.
- Did not touch `app/layout.jsx`, components, `package.json`, or any marketing component.
- Did not change DNS or the domain.

## Next lane

content → https://www.cdsportswearinc.com/hire/shopify-developer
