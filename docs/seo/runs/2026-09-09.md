# Daily Run — 2026-09-09

**Domain: `cdsportswearinc.com` (confirmed by MJ this run). Mode: could not be
checked** — outbound HTTPS is blocked by org policy in this session
(`connect_rejected` on every host), so no Ubersuggest call and no GA/GSC check.
No metric table this run; nothing was measured. The Supabase MCP server was
authorized part-way through, so the post facts below are read from `blog_posts`
directly rather than from the live site.

## What was done

1. **Campaign kits committed** for three posts MJ reports as live —
   `docs/seo/campaigns/{how-much-does-ai-automation-cost,
   branding-and-web-design-studio,web-design-manassas-va}.md`, plus a
   `campaigns/README.md` setting the convention. Copy is MJ's, committed
   verbatim with pre-send checks appended per kit. **Nothing was sent.** MJ
   chose "repo campaign pack only"; sending still needs an explicit yes per
   `../README.md`.
2. **Brand name swept to `CD Sportswear Inc`** across the app front and back
   at MJ's instruction — see the "Brand rename" section below.

## Findings — three live posts are outside the pipeline

Confirmed in `blog_posts` — all four rows are `status: published`:

| Slug | Published (UTC) | Body | Cover |
|---|---|---|---|
| `how-much-does-ai-automation-cost` | 2026-09-06 09:10 | 9,274 ch | none |
| `branding-and-web-design-studio` | 2026-09-06 09:26 | 12,373 ch | none |
| `web-design-manassas-va` | 2026-09-06 09:32 | 14,526 ch | none |
| `web-development-rfp-guide` | 2026-09-03 01:49 | 11,827 ch | none |

**No post has a cover image.** `cover_image_url` is null on every row, so every
share card and blog-listing thumbnail falls back to `SOCIAL_IMAGE_PATH`. The
Canva creatives in `../campaigns/` are the obvious source for four real covers.

None of the three has a draft in `../drafts/blog/`; only
`web-development-rfp-guide.md` is there. So the posts were authored outside the
documented route (drafted elsewhere, or straight into `/admin/blog`).

### Internal links point at a redirecting host

The three new posts carry **51 absolute internal links on the apex**
(`https://cdsportswearinc.com/...`) and **zero** site-relative ones —
`web-design-manassas-va` 23, `branding-and-web-design-studio` 22,
`how-much-does-ai-automation-cost` 6. The apex 308-redirects to `www`
(`lib/seo.mjs`), so **every internal link in these three posts burns a redirect
hop** and none matches the canonical host the rest of the site emits.

`web-development-rfp-guide` — the one post that went through the reviewed
pipeline — has 0 apex links and uses site-relative paths, which is what
`../drafts/blog/README.md` requires precisely "so they stay correct if the
domain ever changes again". The domain has since moved again, which is the
argument making itself.

Not fixed this run: it is a content edit to three live posts and is not what MJ
asked for. Suggested fix, to run once approved:

```sql
update blog_posts
set body = replace(body, 'https://cdsportswearinc.com/', '/')
where body like '%https://cdsportswearinc.com/%';
```

**The publish workflow was never installed.** It is still
`../seo-publish-blog.yml.pending`, not in `.github/workflows/`. Steps 1–3 of
"How a blog post ships" have therefore never run for any post. Either install
it or amend the README to describe what is actually happening — right now the
documented process and the real one disagree, and the README is the thing new
runs are told to trust.

Backfilling drafts for the three would be **safe** —
`scripts/seo/publish-blog-drafts.mjs:169` skips any row already `published`, so
it cannot unpublish or overwrite a live post. Not done this run — it was not
what MJ asked for, and the bodies run to 9–15k characters each.

## Findings — registry drift (logged, not corrected)

Per MJ's decision this run: **log the drift, leave `../KEYWORD-REGISTRY.md`
alone.** The registry is MJ-approved strategy; unparking a term is a strategy
call, not bookkeeping.

| Post | Registry state | Drift |
|---|---|---|
| `/blog/web-design-manassas-va` | `web design manassas va` (20/mo, diff 5) **Parked**, was mapped to `/manassas-va-web-design` | **Conflict.** A live post against a term the CRY-21 national/specificity decision deliberately parked. If `/manassas-va-web-design` is ever built, the two compete — this is exactly what the guard exists to prevent |
| `/blog/branding-and-web-design-studio` | `branding agency northern virginia` (0/mo, diff 4) **"Dropped: no demand (CRY-20)"** | **Conflict.** Live post against a dropped line. The post may well target a different, better term — but whatever that term is, it is not in the registry |
| `/blog/how-much-does-ai-automation-cost` | no row; `ai automation agency` (4,400/mo, diff 35) → `/services/ai-automation` | **Adjacent, not conflicting.** A cost query against a navigational service page. Wants its own row |

**For MJ:** three live URLs are unmapped in the cannibalisation guard. The
decision needed is per post — reaffirm the park and treat the post as
off-strategy, or unpark and remap. Either way the registry should stop being
silent about pages that exist.

## Brand rename — `CD Sportswear USA` → `CD Sportswear Inc`

MJ's instruction: the name is `CD Sportswear Inc` front and back. Swept
`lib/site.js` (`SITE.name`, the single source of truth), every `app/` route's
metadata, `components/sections/`, `lib/{projects,servicePages,clientTileImages}`,
the test assertions, and the living docs. Verified: 493/493 node tests, 36/36
marketing tests, clean `next build` (48 built pages carry the new name).

### Database content — the repo sweep does not reach Supabase

A scan of every text column in the `public` schema found the old name in
exactly two places:

- **`blog_posts.body` — 3 occurrences across 2 live posts**
  (`web-design-manassas-va` ×2, `branding-and-web-design-studio` ×1). All three
  are the studio naming itself in its own prose, so all three are in scope for
  the rename. Applied as:

  ```sql
  update blog_posts
  set body = replace(body, 'CD Sportswear USA', 'CD Sportswear Inc')
  where body like '%CD Sportswear USA%';
  ```

  **Confirmed applied** — re-read after the connector reconnected: all four
  rows return `still_stale: false`, and the two edited rows now carry the new
  name. The site no longer serves the old name from any post body.

- **`deals.description` — 1 occurrence**, deal `75ddbf12`, "Website inquiry —
  Pranab L", 2026-08-27. It is **inbound cold outreach a third party sent to
  the company** through the contact form, opening "Hello CD Sportswear USA".
  **Left alone deliberately.** It records what someone else wrote; editing an
  inbound CRM record to match a later rebrand corrupts the record, for the same
  reason the testimonials stay verbatim.

**Deliberately not renamed** — each needs MJ, and each is a different reason:

- **`lib/reviews.js`** — six occurrences sit inside **verbatim client review
  bodies and one review headline**, including a negative review. Editing a
  customer's own words is falsifying a testimonial; a rebrand is not a licence
  to do it. `/reviews` is the one page still rendering the old name, correctly.
- **`sales@cdsportswearusa.com`** (`lib/site.js`) and
  **`no-reply@cdsportswearusa.com`** (`lib/email/resend.js`) — addresses, not
  names. Changing either breaks mail unless a mailbox exists at the new domain
  and Resend has verified it. The root `CLAUDE.md` already flags this as an
  open owner action.
- **`/cd-sportswear-usa-logo.png` and `-icon.png`** — public URLs. The v1.34
  merge already swapped the *image* to the INC lockup, so the artwork is
  correct and only the filename is stale. Renaming breaks any cached or
  hotlinked URL for cosmetics nobody sees.
- **`supabase/migrations/0001`–`0005`** — first-line SQL comments in migrations
  that have already been applied. Editing applied migration files makes the
  repo disagree with what ran.
- **`CHANGELOG.md`, `docs/seo/runs/2026-09-01.md`, `docs/archive/`,
  `docs/plans/`, `docs/reports/`** — historical records. They describe what was
  true when written and should keep saying so.

## New Ubersuggest project — `cdsportswearinc.com`

MJ created a replacement project late in this run. It is now the only project
on the account, so `../KEYWORD-REGISTRY.md`'s header — "Tracked in Ubersuggest
project `5dfd943c…` (cdsportswearusa.com)" — names a project and a domain that
no longer apply. Left uncorrected pending MJ, along with the rest of the
registry.

| Field | Value |
|---|---|
| Project ID | `109eb16879ab6b871522949b04ab791df52a888ebbab4b2037450214d037b7cf` |
| Domain | `cdsportswearinc.com` |
| Created | 2026-09-09 15:20 UTC |
| Keywords | 50 of 125 — **75 slots free** |
| Locations | 1 of 20 — US (2840), `en` |
| Competitors | 5 of 5 — huemor.rocks, barrelny.com, ironpaper.com, loungelizard.com, bopdesign.com |
| Update frequency | Weekly; next 2026-09-16 |
| Sitemap | `https://www.cdsportswearinc.com/sitemap.xml` — correctly on `www` |
| `google_analytics_profile` | `null` |

**Mode A still holds.** GA is still not connected on the new project, so the
mode gate is unchanged from 2026-09-02 — every figure remains an Ubersuggest
estimate, with no owned analytics to corroborate it.

**Baseline: 0 of 50 keywords rank in the top 100**, old and new columns alike
(`not_ranking: 50`). 24 of the 50 are still `pending` first SERP collection.
This is the expected reading for a domain this young and is worth keeping as
the honest zero-point.

**The brand inside Ubersuggest is still the old name.** `brand.name` and
`business_summary.business_name` both read `CD Sportswear USA`, and the
generated "about" text repeats it three times. It also still describes eight
disciplines and "60+ projects shipped / 10+ years", which matches `lib/site.js`.
Only the name is stale — MJ can correct it in the project's brand settings.

### The tracked set does not match the programme

Two problems, both worth MJ's decision before anything is added or removed.

**1. Not one of the registry's live targets is tracked.** Everything the
programme has actually committed to is absent from the 50:

| Registry target | Vol/mo | Diff | State | Tracked? |
|---|---|---|---|---|
| `hire a shopify developer` | 880 | **8** | CRY-24, next lane | no |
| `hire shopify developer` | 880 | 26 | CRY-24 | no |
| `rfp web development` | 260 | 13 | **post is live** | no |
| `website development rfp` | 260 | 13 | **post is live** | no |
| `web development northern virginia` | 260 | 9 | Next tier | no |
| `ai automation agency` | 4,400 | 35 | service page exists | no |

Nor is any term belonging to the three posts published on 2026-09-06. So the
project cannot currently measure a single thing the programme is working on.

**2. The 50 that are tracked skew unwinnable, duplicated, or irrelevant.**

- **Off-brand entirely:** `hulu logos` (9,900/mo), `dreamwork logo` (9,900/mo)
  — other companies' brand terms. `design a logo canva` (27,100/mo) and
  `n8n workflow` (9,900/mo) are navigational to Canva and n8n. None can be won
  and none would convert if it were.
- **Difficulty far above what a days-old domain can reach:**
  `identity design branding` 85, `digital marketing agency` 82,
  `marketing agency near me` 79, `logo design` 75, `logos and designs` 74.
- **Near-duplicates burning slots:** seven variants of the same intent —
  `digital marketing agency`, `agency marketing digital`, `marketing agency
  digital`, `digital agency marketing`, `digital marketing co`, `digital
  marketing businesses`, `digital marketing firms`. One page can only win one.
- **Scraped-looking grammar:** `and logo design`, `websites designers`,
  `websites designing`, `design logo design`, `designer logo design`.

The set reads volume-sorted rather than chosen. For contrast, the registry's
`hire a shopify developer` is 880/mo at **difficulty 8** — the best
value-to-difficulty ratio in the whole programme, and untracked, while 50 slots
sit on terms at difficulty 50–85.

### Acted on — MJ decided both, same run

**"The registry governs; remove all four off-brand terms."** Applied via
`add_project_keywords`, which replaces the whole map (there is no delete
endpoint — omitting a keyword from the full map is how removal works).

- **Added 7:** `hire a shopify developer`, `hire shopify developer`,
  `rfp web development`, `website development rfp`,
  `web development northern virginia`, `ai automation agency`,
  `branding and web design`.
- **Removed 4:** `hulu logos`, `dreamwork logo`, `design a logo canva`,
  `n8n workflow`.
- **50 → 53 of 125.** Verified in the response: all seven present, all four
  gone. Every registry target is now measured for the first time.

`../KEYWORD-REGISTRY.md` updated accordingly — new project ID and domain in the
header, and a row for `branding and web design` (720/mo, diff 31, CPC $16.50,
navigational, pulled 2026-09-09) mapped to the live branding post. That is not
a reversal of CRY-20, which dropped `branding agency northern virginia` for
having 0/mo demand; this is a different term with real volume.

**The AI-cost post has no viable head term.** `ai automation cost` is
**10/mo at difficulty 45, CPC $0** (US 2840, 2026-09-09), with 0 searches in
eight of the last thirteen months and a peak of 20. Not tracked — a slot spent
there measures nothing. The post still earns its place answering a real sales
question and supporting `ai automation agency` (4,400/mo), but it should not be
treated as a ranking play, and the campaign kit's traffic expectations should
be set accordingly. It needs a better primary term before it gets a registry
row.

**Still unresolved:** the Manassas post's term stays parked, so it remains
untracked. That is the same CRY-21 conflict logged above, still MJ's call.

## Next-run lane

MJ's call on whether the new project's 50 keywords supersede the registry or
the registry still governs — that answer decides the keyword lane and the
registry header both. Then the three registry conflicts, the 51 apex internal
links, the four missing cover images, the stale brand name in Ubersuggest, and
either installing `seo-publish-blog.yml` or correcting the README to match
reality. The
`/hire/shopify-developer` code page (CRY-24, 880/mo, diff 8) is still the
highest-value unbuilt target and has been the stated next lane since
2026-09-02.
