# Daily Run — 2026-09-09

**Domain: `cdsportswearinc.com`. Mode: could not be checked** — outbound HTTPS is
blocked by org policy in this session (`connect_rejected` on every host), so no
Ubersuggest call, no GA/GSC check, and no live URL verification was possible.
No metric table this run; nothing was measured.

## What was done

1. **Campaign kits committed** for three posts MJ reports as live —
   `docs/seo/campaigns/{how-much-does-ai-automation-cost,
   branding-and-web-design-studio,web-design-manassas-va}.md`, plus a
   `campaigns/README.md` setting the convention. Copy is MJ's, committed
   verbatim with pre-send checks appended per kit. **Nothing was sent.** MJ
   chose "repo campaign pack only"; sending still needs an explicit yes per
   `../README.md`.
2. **Brand name swept to `CD Sportswear Inc`** across the app front and back
   at MJ's instruction — see the "Brand rename" section below.

## Findings — three live posts are outside the pipeline

None of the three has a draft in `../drafts/blog/`; only
`web-development-rfp-guide.md` is there. So the posts were authored outside the
documented route (drafted elsewhere, or straight into `/admin/blog`).

**The publish workflow was never installed.** It is still
`../seo-publish-blog.yml.pending`, not in `.github/workflows/`. Steps 1–3 of
"How a blog post ships" have therefore never run for any post. Either install
it or amend the README to describe what is actually happening — right now the
documented process and the real one disagree, and the README is the thing new
runs are told to trust.

Backfilling drafts for the three would be **safe** —
`scripts/seo/publish-blog-drafts.mjs:169` skips any row already `published`, so
it cannot unpublish or overwrite a live post. It was not done here because the
post bodies could not be read: egress is blocked and the Supabase MCP server is
unauthorized in this session.

## Findings — registry drift (logged, not corrected)

Per MJ's decision this run: **log the drift, leave `../KEYWORD-REGISTRY.md`
alone.** The registry is MJ-approved strategy; unparking a term is a strategy
call, not bookkeeping.

| Post | Registry state | Drift |
|---|---|---|
| `/blog/web-design-manassas-va` | `web design manassas va` (20/mo, diff 5) **Parked**, was mapped to `/manassas-va-web-design` | **Conflict.** A live post against a term the CRY-21 national/specificity decision deliberately parked. If `/manassas-va-web-design` is ever built, the two compete — this is exactly what the guard exists to prevent |
| `/blog/branding-and-web-design-studio` | `branding agency northern virginia` (0/mo, diff 4) **"Dropped: no demand (CRY-20)"** | **Conflict.** Live post against a dropped line. The post may well target a different, better term — but whatever that term is, it is not in the registry |
| `/blog/how-much-does-ai-automation-cost` | no row; `ai automation agency` (4,400/mo, diff 35) → `/services/ai-automation` | **Adjacent, not conflicting.** A cost query against a navigational service page. Wants its own row |

**For MJ:** three live URLs are unmapped in the cannibalisation guard. The
decision needed is per post — reaffirm the park and treat the post as
off-strategy, or unpark and remap. Either way the registry should stop being
silent about pages that exist.

## Brand rename — `CD Sportswear USA` → `CD Sportswear Inc`

MJ's instruction: the name is `CD Sportswear Inc` front and back. Swept
`lib/site.js` (`SITE.name`, the single source of truth), every `app/` route's
metadata, `components/sections/`, `lib/{projects,servicePages,clientTileImages}`,
the test assertions, and the living docs. Verified: 493/493 node tests, 36/36
marketing tests, clean `next build` (48 built pages carry the new name).

**Deliberately not renamed** — each needs MJ, and each is a different reason:

- **`lib/reviews.js`** — six occurrences sit inside **verbatim client review
  bodies and one review headline**, including a negative review. Editing a
  customer's own words is falsifying a testimonial; a rebrand is not a licence
  to do it. `/reviews` is the one page still rendering the old name, correctly.
- **`sales@cdsportswearusa.com`** (`lib/site.js`) and
  **`no-reply@cdsportswearusa.com`** (`lib/email/resend.js`) — addresses, not
  names. Changing either breaks mail unless a mailbox exists at the new domain
  and Resend has verified it. The root `CLAUDE.md` already flags this as an
  open owner action.
- **`/cd-sportswear-usa-logo.png` and `-icon.png`** — public URLs. The v1.34
  merge already swapped the *image* to the INC lockup, so the artwork is
  correct and only the filename is stale. Renaming breaks any cached or
  hotlinked URL for cosmetics nobody sees.
- **`supabase/migrations/0001`–`0005`** — first-line SQL comments in migrations
  that have already been applied. Editing applied migration files makes the
  repo disagree with what ran.
- **`CHANGELOG.md`, `docs/seo/runs/2026-09-01.md`, `docs/archive/`,
  `docs/plans/`, `docs/reports/`** — historical records. They describe what was
  true when written and should keep saying so.

## Next-run lane

MJ's call on the three registry conflicts, then either install
`seo-publish-blog.yml` or correct the README to match reality. The
`/hire/shopify-developer` code page (CRY-24, 880/mo, diff 8) is still the
highest-value unbuilt target and has been the stated next lane since
2026-09-02.
