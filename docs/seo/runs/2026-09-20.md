# SEO run — 2026-09-20 (daily) — lane: content

Mode: A (measurement dark) — evidence: GSC UNAVAILABLE (no service-account key at
`cds-seo/gsc-service-account.json`); Bing UNAVAILABLE (`BING_WEBMASTER_API_KEY` not set
in `HERMES_HOME/.env`). Web extraction backend (Firecrawl) also returned 403 this run,
so no fresh web research was possible. All numbers below are unavailable — not estimated.

Goal metric (STRATEGY.md §1 — qualified organic inquiries): unavailable this run —
GA4 read access not wired to Hermes; CRM reconciliation not wired. Next checkpoint
T+90 2026-12-02 ≥1 qualified organic inquiry.

Operator: Hermes cron `CDS-SEO daily` · model: upstage/solar-pro4:free (provider nous) ·
workdir: C:\Users\moizjmj\CD Sportswear USA\site

## Publication check
- Live site re-fetched this run (pre-run `cds_seo_live.py`): homepage, 8 service pages,
  /work, /contact, /blog all HTTP 200, canonical on `www.cdsportswearinc.com`, robots-meta
  `index, follow`. No new live URLs since the last run.
- Items shipped in open PR #201 (v1.39, seo/2026-09-22-ai-visibility, OPEN) are **not yet
  live** because the PR is unmerged and `main` is already at v1.41 (commit bc1a266). Confirmed:
  `/llms.txt` → 404 on live, no IndexNow key file on live, `/services/seo` not on live.
- `/hire/shopify-developer` correctly absent (parked in registry).

## Measurement
Measurement (GSC 28d to 2026-09-20): UNAVAILABLE — see Mode line above.
Bing (week of 2026-09-20): UNAVAILABLE — see Mode line above.
Movements worth noting: none measurable this run.

## Work shipped
- **Content draft** — `docs/seo/drafts/blog/how-to-write-a-web-design-rfp.md`
  - Target keywords: `web design rfp`, `how to write a web design rfp` (both added to
    KEYWORD-REGISTRY.md mapped table; figures marked unavailable — not yet in Ubersuggest
    project `109eb168…`, so not re-pulled; flagged for the next Keywords lane to track)
  - Target URL: `/blog/how-to-write-a-web-design-rfp`
  - Approved: `false` (MJ-only gate per drafts/README)
  - Supports `/services/web-design` pillar (Theme 1); cross-links to the existing
    `/blog/web-development-rfp-guide` (which owns the generic development-RFP terms) and to
    `/services/web-design`, `/process`, `/contact`
  - Contains: front-matter per drafts/README, opening that answers the search intent in the
    first paragraph, 9 `##`/`###` sections, a copy-paste RFP template, a 6-criterion scorecard,
    a "before you send it" checklist, 5 call questions, and a closing CTA with 3 internal links
  - No invented numbers, clients, or outcomes
  - Files: `docs/seo/drafts/blog/how-to-write-a-web-design-rfp.md` + `docs/seo/KEYWORD-REGISTRY.md`
  - Verified by: draft read against drafts/README spec (front-matter keys, body Markdown subset,
    internal-link paths are site-relative)

## Findings (evidence-backed)
- **PR #201 is stale relative to production.** It bumps VERSION to v1.39 and carries
  llms.txt, IndexNow key + ping script, robots.js AI-crawler allow rules, `/services/seo`
  page, 12 re-scored prospects, keyword registry + ops manual updates, and run logs across all
  five lanes (Sept 22–24). `main` is already at v1.41 (bc1a266, "clean stale identity, domain,
  and migration claims across agent docs"). Until #201 merges or is rebased, those items are not
  live — confirmed by fetching `/llms.txt` (404) and the live sitemap (no `/services/seo` loc)
  this run.
- **Two target keywords for the new draft are not yet tracked.** `web design rfp` and `how to
  write a web design rfp` are not in Ubersuggest project `109eb168…` (54/125 keywords). Their
  volume/difficulty/CPC are marked unavailable in the registry, not estimated. The next Keywords
  lane should decide whether to add them to the project (needs MJ's yes per §4).
- **The changelog in PR #201 claims `how-to-write-a-web-design-rfp.md` was added, but that file
  was not in the PR's file list** (only `technical-seo-audit-checklist.md` was). This run
  delivers the file that PR #201's own changelog said it had.

## Blocked on MJ
- **Merge / rebase PR #201.** It carries real, verified SEO assets (llms.txt, IndexNow, AI-crawler
  robots rules, `/services/seo` page) that are not live because it is unmerged and stale against
  main. MJ merges; the agent does not.
- **GSC + Bing API access for Hermes.** Until the service-account key and Bing Webmaster API key
  are in place, the daily job is Mode A and cannot report first-party clicks/impressions/positions.
  This blocks the goal metric and the measurement block every run.
- **Track the two new keywords.** Adding `web design rfp` / `how to write a web design rfp` to
  the Ubersuggest project needs MJ's explicit yes (STRATEGY.md §4).

## Next lane
Technical — audit the current live site (v1.41) for regressions since the last technical run.
The last technical work in PR #201 was against an earlier build; the live site has since moved to
v1.41. Verify llms.txt, IndexNow, robots.txt AI rules, sitemap coverage, and service-page metadata
on the current production build, and note anything that regressed.
