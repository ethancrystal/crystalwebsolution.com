# Daily Run — 2026-09-02

**Domain: `cdsportswearusa.com`. Mode: A** (GA not connected —
`google_analytics_profile: null` on the Ubersuggest project; GSC not verified).
MJ was present for most of this run, so several decisions were made live.

## Metric table

| Metric | Source | 2026-08-17 | 2026-09-02 | Note |
|---|---|---|---|---|
| cdsportswearusa.com — everything | Ubersuggest `domain_overview`, 2840 | — | `noData` | Domain is 6 days old. Expected |
| crystalwebsolution.com — DA | Ubersuggest `domain_overview`, 2840 | 7 | 9 | Inflated by PBN spam. Not progress |
| crystalwebsolution.com — backlinks | same | 146 | 212 | ~66 are PBN |
| crystalwebsolution.com — referring domains | same | 13 | 45 | ~32 are PBN |
| crystalwebsolution.com — organic keywords | same | 1 | 3 | retired domain |
| Tracked keywords, new project | Ubersuggest `get_project` | — | 6 / 125 | seeded today |

No GA4 or Search Console numbers exist. All figures above are Ubersuggest
estimates.

## What was done

1. **App-URL fix verified.** Vercel build log for `dpl_DVsW1SzvPwWEUNFTxKf3AXrSANTR`
   (production, 10:36 UTC): `Skipping build cache, deployment was triggered
   without cache.` Clean rebuild of `26e678b`, so `NEXT_PUBLIC_APP_URL` was
   inlined fresh. CRY-28 item 3 can be struck.
2. **PBN blast found on the old domain** — `linking_domains` returned 30 new
   referring domains, all one template. Filed CRY-29 (Urgent). **Reversed the
   2026-09-01 recommendation to 301 the old domain.** MJ confirmed nobody
   bought a link package. Decision: leave the old domain dark. Details in
   `backlinks/pbn-watch.md` and Operations Manual §7.
3. **Ubersuggest project replaced.** MJ deleted the old `crystalwebsolution.com`
   project (inactive/invalid_domain). Run created `cdsportswearusa.com`
   (`5dfd943c…`, en/2840) and, on MJ's "seed it", added six keywords. All
   figures re-pulled live at seed time; only drift since 08-21 is
   `ai automation agency` difficulty 38 → 35. CRY-20 closed.
4. **Notion dropped as memory.** MJ's call. The connector serves a limited MCP
   (four tools, no fetch/search/create) and the Zapier route is out of tasks.
   This directory replaces it. Operations Manual reconstructed as v2, clearly
   marked.
5. **Blog publishing pipeline built** — `drafts/blog/` + `scripts/seo/publish-blog-drafts.mjs`
   + `.github/workflows/seo-publish-blog.yml`. Approved drafts merged to main
   land in `blog_posts` as `draft`; MJ publishes from `/admin/blog`. Inert
   until repo secrets exist.

## Lane worked

Publication pipeline (the build above). No content draft written — the run
spent its budget on the domain decision and the memory migration.

## Drafts awaiting MJ approval

None.

## Open PRs awaiting merge

This scaffold PR. Plus, unrelated to SEO: #163, #162, #149, dependabot #152–158.

## Verification

The 2026-09-01 verifier's corrections stand. Today's Ubersuggest figures were
pulled directly and are quoted verbatim; no independent verifier was run on
the PBN finding because the pattern is unambiguous in the raw data (same path,
title and anchor on all 30), and MJ's answer settled the interpretation.

## Blocked on MJ

See Operations Manual §11. Newest items: remove old domains from Vercel (they
were re-attached today and are inert only because DNS is dead); add the
workflow secrets; name the covers bucket; pick competitors.

## Next-run lane

**Content — the RFP guide (CRY-22)** as `drafts/blog/web-development-rfp-guide.md`.
It is the first linkable asset, it targets two tracked terms, and it exercises
the new pipeline end to end. Then the Shopify landing page (CRY-24) as a code PR.

## GitHub write access — blocked

Pushing the scaffold branch failed twice: the session's git proxy refuses the
repo (`access denied by the git proxy … not in this session's authorized
repository set`) and the GitHub MCP connector returns `403 Resource not
accessible by integration` on `POST /git/refs`. The connector is read-only for
this repository — the same error CRY-18 recorded on 2026-08-17. The run cannot
open PRs until that changes. The scaffold was delivered to MJ as a
`git am`-able patch and a zip instead.

**To unblock (MJ):** re-authorise the GitHub connector with *Contents: write*
and *Pull requests: write* on `ethancrystal/crystalwebsolution.com` (a
fine-grained token or the GitHub App's repository access settings).

---

## Run 2 — content lane (MJ asked for the next run at ~13:45 UTC)

**Mode A** re-confirmed: `get_project 5dfd943c…` → `google_analytics_profile: null`;
`domain_overview cdsportswearusa.com` → `noData` (expected, domain age).

**Artifact:** `docs/seo/drafts/blog/web-development-rfp-guide.md` — ~2,100
words, `approved: false`. Targets `rfp web development` and `website
development rfp` (both 260/mo, diff 13, US 2840, re-pulled 2026-09-02).
Validated through `scripts/seo/publish-blog-drafts.mjs --dry-run` with
`approved` temporarily true: passes `blog-contract.mjs`, no H1, three internal
links to routes that exist (`/contact`, `/services/web-design`, `/process`).

**Verifier (independent agent):** SHIP WITH REQUIRED CHANGES → all five
applied. It (1) retargeted the service link from `/services/web-development`
(internal-tools positioning) to `/services/web-design`; (2) had the
"we read RFPs, price them, decline them" claim softened pending MJ; (3) had the
"no-obligation review" promise removed pending MJ; (4) fixed a template
instruction that only made sense inside the blog; (5) required the
technical/non-functional requirements section that every top-ranking
competitor covers. Also took: shorter `seo_title` for SERP truncation, single
CTA, one more use of the second target term, the nine-vs-eleven reconciliation.

**Defect found on the way: CRY-30 (Urgent).** All eight `/services/[slug]`
pages render `[CONFIRM: …]` editing notes live — `deliverablesNote` plus two
FAQ answers each, and the FAQ ones are in `FaqSchema` JSON-LD. Verified on
`/services/web-design` 2026-09-02. Open PR #162 does *not* fix these (it never
touches `lib/servicePages.mjs`). Sequence that fix before publishing the RFP
guide, which links there.

**GitHub write:** still `403 Resource not accessible by integration`.
Delivered as a refreshed patch (this draft rides in the same commit as the
scaffold) rather than a PR. CRY-18 commented.

**Next-run lane:** if MJ approves the RFP draft — publication pipeline
(confirm the workflow ran, row landed as draft). Otherwise content again:
`/hire/shopify-developer` (CRY-24) as a code-page PR, after reading
`app/embroidery-screen-printing-web-design/page.jsx` as the closest
existing long-form landing page for structure.
