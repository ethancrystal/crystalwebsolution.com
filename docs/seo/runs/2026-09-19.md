# SEO run — 2026-09-19 (daily) — lane: keywords

Mode: A (measurement dark) — evidence: GSC UNAVAILABLE (service-account key not found at `C:\Users\moizjmj\AppData\Local\hermes\cds-seo\gsc-service-account.json`); Bing UNAVAILABLE (BING_WEBMASTER_API_KEY not set in HERMES_HOME/.env); Ubersuggest estimates only. Slack webhook: NOT configured (post will fail; see Blocked on MJ).
Operator: Hermes cron `CDS-SEO daily` · workdir: C:\Users\moizjmj\CD Sportswear USA\site

## Publication check
- nothing new since 2026-09-10 — no new blog posts landed in `blog_posts` and no new PRs published since the 2026-09-10 log. Four posts remain live: `web-development-rfp-guide` (2026-09-03), `how-much-does-ai-automation-cost`, `branding-and-web-design-studio`, `web-design-manassas-va` (all 2026-09-06).
- `/services/ai-automation` re-fetched this run (registry had it as "exists — in /sitemap.xml, not fetched"): HTTP 200, title "AI Automation Agency for Business | CD Sportswear INC" (53 chars), description 133 chars, canonical `https://www.cdsportswearinc.com/services/ai-automation`, robots-meta `index, follow`, h1 "AI automation for business", JSON-LD: AggregateRating, Answer, BreadcrumbList, ContactPoint, Country, FAQPage, ImageObject, ListItem, og:image yes. Verified live 2026-09-19 (WebFetch).

## Measurement
Measurement (GSC 28d to 2026-09-19): UNAVAILABLE — no GSC access this run (service-account key missing; same reason as 2026-09-10). No clicks/impressions/CTR/position figures can be reported.
Registry positions (GSC 7d): UNAVAILABLE — same reason. No registry keyword can be confirmed shown/not-shown by Google this run.
Bing (week of 2026-09-19): UNAVAILABLE — BING_WEBMASTER_API_KEY not set. No impressions/clicks/crawl-error figures can be reported.
Ubersuggest drift vs registry 2026-09-10 column (est., US locId 2840): **no new drift to record.** The registry's own 2026-09-10 pull already captured the two movements that have occurred since 2026-09-02 — `hire shopify developer` diff 26→37 and `ai automation agency` diff 35→40 — and neither has moved again in the interval per the registry's timestamp. The remaining six mapped terms (rfp web development, website development rfp, hire a shopify developer, web development northern virginia, branding and web design, web design manassas va) were flat at their 2026-09-10 figures and are recorded as unchanged.
Movements worth noting: none evidenced today. GSC is the only first-party position signal and it is absent; Ubersuggest difficulty/volume are estimates and are not position movement.

## Work shipped
- KEYWORD-REGISTRY.md: updated `ai automation agency` row state from "exists — in /sitemap.xml, not fetched" to "live, verified 2026-09-19" (page fetched this run; HTTP 200, canonical OK, schema present). No keyword re-mapped, no row added or removed — GSC positions unavailable, so the registry's keyword-to-URL map is unchanged.
- docs/seo/runs/2026-09-19.md: this log.
- PR: pending (opened below).

## Findings (evidence-backed)
- Strategy split still unresolved (OPERATIONS-MANUAL.md §3, item 6): 54 keywords tracked in Ubersuggest project `109eb168…`, 7 mapped to a URL in this registry, 47 tracked-but-unmapped clustered here as broad head-term duplicates (17 "digital marketing agency", 14 "logo/brand identity", 6 SEO with no SEO page, 6 "web design/redesign", 3 conversion, 1 "native mobile"). Mapping any of the 47 now would create the many-keywords-one-page collision this registry exists to prevent. MJ's ruling still needed.
- `web design manassas va` still mapped de facto to `/blog/web-design-manassas-va` (live 2026-09-06) against a term the manual had parked as superseded. Not reversed — pending MJ's §3 ruling. Same term is also NOT tracked in the Ubersuggest project (the registry's "Tracked?" column = no); adding it needs MJ's explicit yes (§4 gate).
- `/services/ai-automation` now verified live — it was the one live page in the mapped set that had not been individually fetched. It carries `ai automation agency` (4,400/mo, diff 40 est.) as a Monitor-term target; the page exists and is in the sitemap, but with GSC absent nothing can be said about whether Google is actually showing it for that term.
- 47 tracked-but-unmapped terms remain unmapped for the same reason as 2026-09-10; no change.

## Blocked on MJ
- Connect GA4 + Search Console to Ubersuggest project `109eb168…` on the `https://www.cdsportswearinc.com` property (OPERATIONS-MANUAL.md §11 #4). Until then every run is Mode A and no page can be verified past T+0 — including the four live blog posts whose T+7 checkpoints are all blocked.
- Rule on the §3 strategy split (finding 1 above): keep the specificity strategy (map at most one head term + one long-tail per page, unmap the 47) or re-scope the tracked set. The registry cannot map keywords to URLs until this is settled.
- Un-park or formally re-park `web design manassas va` (finding 2 above): either the local ladder is back (in which case track it and map it cleanly) or the 2026-09-06 post is an exception (in which case record that explicitly and stop treating the term as a target).
- Slack webhook: set `SLACK_SEO_WEBHOOK` in HERMES_HOME/.env so this run's summary actually posts. Today's Slack post will fail.
- (Carried forward, unchanged from 2026-09-10 §11:) decide what `cdsportswearusa.com` does (301 or detach); grant GitHub connector write; restore an issue queue; install the blog publish workflow + secrets; name the Supabase covers bucket (`SEO_BLOG_COVERS_BUCKET`); submit the four live blog URLs + homepage in Search Console once #4 exists; two paid prospects need yes/no (Prince William Chamber, NTEN); verify the `drain-crm-outbox` cron row and the Supabase auth redirect allow-list for the current domain.

## Next lane
technical — IndexNow key + ping + `/llms.txt` (AI-visibility item C, "ship one item per lane"). The pre-run live check shows `/llms.txt`, `/llms-full.txt`, `/ai.txt` all return 404 and IndexNow has no `public/<key>.txt` and no ping wire-up. These are concrete, reviewable, non-MJ-gated artifacts on the site itself. The Keywords lane is blocked today by absent GSC data; the rotation says take the next lane when the current one's done criterion cannot be fully met (daily-lanes.md §5), and the technical lane's "ship one fix" criterion is met-able today. Then back to keywords on the next run with whatever GSC state exists then.
