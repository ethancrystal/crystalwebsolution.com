node_modules/
.next/
.env
.env.local

# Dev-server / tooling output logs, e.g. .crm-dev.stdout.log
*.log

# Local browser QA and editor state
.playwright-mcp/
.zcode/
/.claude/settings.json
/.claude/settings.local.json
/.claude/workflows/

# Temporary logo inspection exports
/logo-preview.png
/public/logo-cropped.png
/public/temp-logo-preview.png

# Local linked worktrees and recoverable cleanup staging
/.worktrees/
/.claude/worktrees/
/_to_delete/

# Google Drive desktop upload staging. Drive creates this inside the working
# tree and fills it with six-figure counts of numeric temp files, which makes
# `git status` crawl. Drive owns these — ignore them, never commit them.
/.tmp.driveupload/
.vercel
.env*
!.env.example
!.envrc


.hermes/
.direnv/

.copilot-tracking/
*.docx
.env.*
*.env
credentials.json
*.pem
*.key
.credentials

# Supabase CLI local state. .temp/start-secrets holds generated local TLS
# private keys and container env files — never commit these.
supabase/.branches
supabase/.temp
